import { VueConstructor } from "vue";

/**
 * The `DocumentsCard` component is a Vue component that represents a card displaying documents.
 */
export const DocumentsCard: VueConstructor = {
  // Component definition
};

/**
 * The `Documents` component is a Vue component that represents a list of documents.
 */
export const Documents: VueConstructor = {
  // Component definition
};